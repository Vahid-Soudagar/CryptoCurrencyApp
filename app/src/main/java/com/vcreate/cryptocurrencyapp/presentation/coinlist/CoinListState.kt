package com.vcreate.cryptocurrencyapp.presentation.coinlist

import com.vcreate.cryptocurrencyapp.domain.model.Coin

data class CoinListState(
    val isLoading: Boolean = false,
    val coins: List<Coin> = emptyList(),
    val error: String = ""
)
